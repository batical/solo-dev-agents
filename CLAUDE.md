# Claude Code Instructions

Use `AGENTS.md` as the primary shared instruction source.

Specialist definitions live in `agents/`.

Only load or invoke a specialist when the task benefits from it.

Do not invoke all agents by default.
